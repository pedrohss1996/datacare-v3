<div class="flex min-h-screen w-full">
    
    <div class="hidden lg:flex w-1/2 relative overflow-hidden">
        <div class="absolute inset-0 bg-cover bg-center" 
             style="background-image: url('/img/login-bg.png');">
        </div>
        <div class="absolute inset-0 bg-gradient-to-br from-blue-900/40 via-transparent to-sky-200/30"></div>

        <div class="relative z-10 p-16 flex flex-col justify-center h-full text-white">
            <h1 class="text-5xl font-bold mb-6 leading-tight">Gestão Hospitalar Inteligente</h1>
            <p class="text-lg text-blue-50 max-w-md mb-10 leading-relaxed">
                Acesso seguro ao prontuário eletrônico, gestão de leitos e painéis analíticos<br>
                para uma saúde mais eficiente.
            </p>
            <span class="inline-flex w-fit px-3 py-1.5 text-xs font-semibold bg-white/20 rounded-full backdrop-blur-sm border border-white/30 text-white">V2.0</span>
        </div>
    </div>

    <div class="flex-1 flex items-center justify-center p-8 sm:p-12 md:p-16 bg-slate-50">
        <div class="w-full max-w-md bg-white rounded-[2rem] shadow-[0_8px_30px_rgb(0,0,0,0.04)] p-10 sm:p-12 border border-slate-100">
            
            <div class="text-center mb-10">
                <div class="mb-6">
                    <img src="/img/Logotipo.png" 
                        alt="Logo DataCare" 
                        class="w-auto h-auto"> 
                </div>
                <h2 class="text-3xl font-bold text-slate-800 mb-3">Bem-vindo</h2>
                <p class="text-slate-500">Faça login para acessar o painel clínico.</p>
            </div>

            <% if (locals.erro) { %>
                <div class="mb-6 p-4 rounded-xl bg-red-50 border border-red-100 text-red-600 text-sm text-center">
                    <%= erro %>
                </div>
            <% } %>

            <form action="/login" method="POST" class="space-y-6">
                
                <div>
                    <label for="usuario" class="block text-sm font-medium text-slate-700 mb-2">Usuário ou Matrícula</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
                        </div>
                        <input type="text" id="usuario" name="usuario" required 
                               class="w-full pl-12 pr-4 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors outline-none text-slate-800 placeholder-slate-400"
                               placeholder="Ex: m.braga">
                    </div>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-slate-700 mb-2">Senha</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
                        </div>
                        <input type="password" id="password" name="senha" required 
                               class="w-full pl-12 pr-12 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors outline-none text-slate-800 placeholder-slate-400"
                               placeholder="••••••••">
                        <button type="button" id="togglePassword" class="absolute inset-y-0 right-0 pr-4 flex items-center text-slate-400 hover:text-slate-600 transition-colors focus:outline-none">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                        </button>
                    </div>
                    <a href="#" class="block mt-2 text-sm font-medium text-blue-600 hover:text-blue-700 transition-colors">Esqueceu a senha?</a>
                </div>

                <button type="submit" class="w-full py-3.5 px-6 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl shadow-sm transition-all duration-200 transform hover:-translate-y-0.5 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                    Acessar Sistema
                </button>

            </form>

        </div>
    </div>

</div>

<script>
    // Seu script original de Toggle Password
    const togglePassword = document.getElementById('togglePassword');
    const passwordInput = document.getElementById('password');
    // const eyeIcon... (removido pois o ícone SVG está hardcoded no HTML, mas a lógica abaixo funciona)

    togglePassword.addEventListener('click', function () {
        const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
        passwordInput.setAttribute('type', type);
        this.classList.toggle('text-blue-600');
    });
</script>